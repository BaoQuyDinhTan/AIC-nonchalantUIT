# AIC-nonchalantUIT 🐧

## Cách hoạt động

Code chính backend các thứ nằm trong **app.py** (100% GPT 🐧 và Asamai làm tester), html nằm trong folder templates (tính chỉnh lại cái này mà lười quá).

Sử dụng conda để tải các thư viện cần thiết (là gì quên cmnr 🐧 có thể hỏi AI để được hỗ trợ phần này nếu cài đúng thì không cần chỉnh code gì vẫn chạy được) (sẽ có document phần này rõ hơn).

## Thư mục
Bỏ các dữ liệu cần thiết vào (giống format của btc) có thêm feature tự extract ra để chạy trên các mô hình khác.
<img width="277" height="216" alt="image" src="https://github.com/user-attachments/assets/c8cddc6f-c2ff-425e-babd-acf42aa5e17e" />
## Các code còn lại
Các code như ``convert_av1.py``, ``extract-features.py``, ``extract-fps.py``, ``extract-frames.py``, ``filter_similar_frames.py`` để hỗ trợ việc lấy thêm frames từ video, lọc frames và lấy features (theo tui nhớ thì tui tích hợp luôn cái lưu features vào trong cái code ``extract-frames.py`` luôn, code ``extract-features.py`` thì nó thực hiện riêng chức năng của nó thôi).
